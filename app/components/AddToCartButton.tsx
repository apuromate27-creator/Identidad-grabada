
"use client";
import Link from "next/link";
import {useState}from"react";
import {useCart}from"./CartContext";
export function AddToCartButton({slug,name,price,priceValue}:{slug:string;name:string;price:string;priceValue:number}){const{addItem}=useCart();const[personalization,setPersonalization]=useState("");const[added,setAdded]=useState(false);const add=()=>{addItem({slug,name,price,priceValue,personalization});setAdded(true);setTimeout(()=>setAdded(false),1800)};return <div className="bg-[#0b0b0b] border border-white/10 rounded-[2rem] p-6 space-y-4"><h2 className="text-2xl font-black">Personalización del grabado</h2><p className="text-zinc-500 text-sm">Escribí el nombre, frase o idea. Luego coordinamos el diseño final antes de grabar.</p><input value={personalization} onChange={e=>setPersonalization(e.target.value)} placeholder="Ej: Juan Pérez, logo, iniciales..." className="w-full bg-black border border-white/10 rounded-2xl px-4 py-4 outline-none focus:border-[#b68b52]"/><div className="grid sm:grid-cols-2 gap-3"><button onClick={add} className="w-full bg-[#b68b52] text-white py-5 rounded-2xl font-black hover:scale-[1.02] transition">{added?"Agregado ✓":"Agregar al carrito"}</button><Link onClick={add} href="/checkout" className="w-full text-center border border-[#b68b52] text-[#d6b17a] py-5 rounded-2xl font-black hover:bg-[#b68b52]/10 transition">Comprar ahora</Link></div></div>}
